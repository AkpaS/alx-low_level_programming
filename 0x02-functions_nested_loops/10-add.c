#include "main.h"

/**
 * add - adds two integers and returns the result
 *
 * @y: the integer y
 * @x: the integer x
 * Return: returns result
 */
int add(int x, int y)
{
	int sum = y + x;

	return (sum);
}
