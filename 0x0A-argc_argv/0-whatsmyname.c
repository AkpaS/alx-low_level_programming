#include <stdio.h>

/**
 * main - writes a program that prints its name
 *
 * @argc: argument count
 * @argv: array of argument strings
 *
 * Return: void
 */

int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
